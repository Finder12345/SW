{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run MapleClaw agent",
      "type": "python",
      "request": "launch",
      "module": "MapleClaw.src.agent.claw",
      "cwd": "${workspaceFolder}/..",
      "envFile": "${workspaceFolder}/.env",
      "console": "integratedTerminal"
    }
  ]
}
